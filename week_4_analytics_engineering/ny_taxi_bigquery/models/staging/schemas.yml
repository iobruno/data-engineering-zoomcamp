version: 2
sources:
  - name: bq-staging-nyc-trip_record

    # GCP Project ID
    database: iobruno-gcp-labs

    # BigQuery Dataset name
    schema: stg_nyc_trip_record_data

    # Tables to source data from
    tables:
      - name: fhv
      - name: green
      - name: yellow
