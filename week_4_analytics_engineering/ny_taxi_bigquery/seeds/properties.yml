version: 2

seeds:
  - name: taxi_zone_lookup
    config:
      enabled: true
      full_refresh: true
      column_types:
        LocationID: numeric
        Borough: string
        Zone: string
        service_zone: string
